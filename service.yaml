apiVersion: v1
kind: Service
metadata:
  name: users
  labels:
    backend-id: users
    region: asia-southeast1
spec:
  selector:
    app: users
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080